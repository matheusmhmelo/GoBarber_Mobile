import LinearGradient from 'react-native-linear-gradient';
import styed from 'styled-components/native';

export default styed(LinearGradient).attrs({
  colors: ['#7159c1', '#ab59c1'],
})`
  flex: 1;
`;
